'use babel';

import TemplatizeStringView from '../lib/templatize-string-view';

describe('TemplatizeStringView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
